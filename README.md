1. to build for prod

```
docker compose up --build
```


2. to build for dev
```
docker compose -f docker-compose.dev.yml up --build
```

3. schema definitions

```
link:
    - name type UNIQUE

acct
```