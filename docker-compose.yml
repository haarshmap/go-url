services:
  app:
    build: .
    ports:
      - "${PORT}:8080"
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_started
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  